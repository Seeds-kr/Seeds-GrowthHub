# 설계 05 — 제품 경험 (편집 · 템플릿 · 알림 · 반응형)

> 상태: **구현 완료** — 편집기·회의록 템플릿(W9), 알림(W10), 반응형 등급(W11) 모두 완료 · 결정: ADR-005 ~ ADR-008
>
> 구현 중 확인된 사항: 회의록에는 원래 **편집 기능이 아예 없었다**(생성만 가능). W9에서 섹션별 인라인 편집을 함께 추가했다.
>
> W11 구현 위치: 등급 대장은 [`artifacts/seeds/src/lib/responsive-tiers.ts`](../../artifacts/seeds/src/lib/responsive-tiers.ts)(라우트 85개 전량 분류),
> 가드는 `components/DesktopOnly.tsx` + `hooks/use-desktop.tsx`.
> **런타임 미검증** — 이 환경에 브라우저가 없어 375px 실측과 드래그 동작은 확인하지 못했다. §8 수용 기준의 마지막 두 항목이 여기 걸려 있다.
> 선행: [00 완성 형상](00-target-state.md) · [baseline/02-core-v2.md](../baseline/02-core-v2.md) §8 · [baseline/04-ops-v3.md](../baseline/04-ops-v3.md) §4·§6
> 대응: 01~04가 비워둔 **UX 축**

---

## 1. 이 문서가 채우는 공백

01~04는 데이터·권한·API를 정했지만 **"이걸 쓰는 경험이 어떤가"** 는 정하지 않았다. 그 결과 코드에는 아무도 결정한 적 없는 답이 굳어 있었다.

| 모듈 | 결정된 적 없이 굳어 있던 것 |
|---|---|
| 회의록 | 고정 4섹션 + plain `Textarea` + `react-markdown` 렌더 |
| 문서 | 단일 마크다운 + 좌우 분할 + 버전 사이드바 |
| 작업 | 칸반처럼 보이지만 드래그 없음 (Select로 상태 변경) |
| 액션 아이템 | 회의록 본문이 아니라 별도 다이얼로그에서 생성 |
| **알림** | **아예 없음** — 대시보드를 열지 않으면 아무도 모름 |

이 문서가 그 공백을 메운다.

---

## 2. 결정 기록

### ADR-005 — 마크다운을 유지하되 툴바를 붙인다

**결정.** 저장 형식은 마크다운 그대로. 그 위에 서식 툴바·단축키·이미지 붙여넣기 업로드를 얹는다. 블록 에디터(Tiptap/BlockNote 등)는 도입하지 않는다.

**근거.** Core v2 §8.1이 마크다운을 source of truth로 삼은 이유 — 버전 이력, 템플릿 복제, PDF/DOCX export, 체크리스트 결합 — 이 전부 블록 에디터를 도입하면 다시 설계해야 한다. 툴바는 그 자산을 하나도 건드리지 않으면서 진입장벽만 없앤다.

**대가.** 노션에 익숙한 사람에게는 여전히 한 단계 낮은 경험이다. 표 편집이 특히 불편하다. → 표는 툴바에서 골격을 삽입해주고, 정말 서식이 중요한 문서는 Drive 링크로 붙인다(Core v2 §9.1의 hybrid 전략).

---

### ADR-006 — 회의록은 유형별 템플릿, 결정·액션은 공통 강제

**결정.** `meetings.meetingType`(general·ops·planning·retro·mentor·external)에 따라 섹션 프리셋을 다르게 채운다. 단, **결정사항과 액션 아이템은 모든 템플릿에 공통으로 강제**한다.

**근거.** 고정 4섹션은 기획 회의·회고에 맞지 않고, 완전 자유는 결정사항 추출을 작성자 역량에 맡기게 된다. 유형별 템플릿은 둘 사이다. 결정·액션만 공통으로 두면 인수인계와 감사에 필요한 최소 구조는 항상 보장된다.

**대가.** 템플릿 7종(`MEETING_TYPES` 전량)을 유지보수해야 한다. → `documents`의 `is_template` 구조를 재사용해 운영진이 직접 고칠 수 있게 한다. 코드에 하드코딩하지 않는다.

---

### ADR-007 — 알림은 Discord 웹훅 + 인앱 배지

**결정.** 운영진 공개 채널에 웹훅으로 알리고, 인앱은 배지로 보완한다. 이메일은 지금 쓰지 않는다.

**근거.** Seeds는 이미 Discord에서 산다. 도달률이 가장 높고, 멘토가 데스크톱 앞에 없을 때도 신호를 받는다(§5.4 참조 — 모바일을 포기한 결정을 상쇄한다).

**경계 — 이게 감시가 되지 않게 하는 규칙:**
- **공개 채널에만** 보낸다. 개인 DM 없음.
- 학생 개인정보·평가정보·회고 내용을 **절대 페이로드에 넣지 않는다.**
- 알림은 "무슨 일이 있다 + 링크"까지만. 내용은 GrowthHub에서 권한 확인 후 본다.
- 학생 개인에게 가는 알림은 만들지 않는다. 운영진·멘토 대상만.

**대가.** Discord가 죽으면 알림이 끊긴다. → 인앱 배지가 항상 병행되므로 GrowthHub 자체 기능은 멈추지 않는다(외부연계 §12 원칙).

---

### ADR-016 — 다크모드는 시스템을 따르되 선택이 이긴다 (2026-08-15)

**결정.** 라이트/다크를 지원한다. 기본은 **OS 설정을 따르고**, 사용자가 헤더에서
직접 고르면 그 선택이 이긴다. 안 골랐다면 OS 가 바뀔 때 계속 따라간다.

**근거.** 스타일은 **이미 다 있었다** — `.dark` 변수 블록과 컴포넌트 65곳의
`dark:` 유틸. 없던 건 `.dark` 클래스를 붙이는 주체 하나뿐이라 그 전부가 죽어
있었다(이슈 #16). 절반만 만들어 둔 것을 마저 잇는 쪽이 지우는 쪽보다 싸다.

`next-themes` 는 쓰지 않는다. 필요한 건 "클래스 하나를 토글하고 기억한다" 가
전부이고, 첫 페인트 전에 정해야 해서 어차피 인라인 스크립트가 필요하다.

**경계 — 켜고 나서야 드러난 것:**
- 넓은 면을 채우는 색과 강조색을 **같은 토큰으로 쓰지 않는다.** `--primary` 의
  다크 값은 강조용 밝은 초록(85% 명도)이라, CTA 띠에 쓰면 어두운 페이지에
  창백한 판이 박힌다. `.brand-band` 를 따로 뒀다.
- `.dark` 에 빠진 변수는 밝은 값을 **상속한다.** `--button-outline` 이
  `rgba(0,0,0,…)` 인 채로 남아 어두운 면 위의 검은 테두리가 됐다.

**대가.** 이제 색을 바꿀 때 두 모드를 같이 봐야 한다. 공개 화면 대비는 두 모드
모두 실측으로 확인한다(라이트 4.83:1 · 다크 7.81:1).

---

### ADR-017 — 프로필 사진은 본인이 올린다 (2026-08-19)

**결정.** AI 아바타 생성을 걷어내고 **파일 업로드**로 바꾼다. 학생·멘토는 자기
사진을, 어드민은 아무 프로필의 사진을 올린다. 주소 직접 입력도 남긴다.

**근거.** AI 생성은 **두 군데가 동시에 막혀** 있었다 — 저장은 Replit 사이드카
(`127.0.0.1:1106`), 생성은 Gemini 프록시(`AI_INTEGRATIONS_GEMINI_*`). 둘 다 이
서버에 없어 기능이 죽어 있었고, 살리려면 과금 계정과 공개 서빙 경로가 새로
필요했다. 얻는 것은 "AI 가 그린 프로필 그림" 하나인데, 사람 얼굴 사진은 어차피
본인 것이 맞다. 로컬 모델로 대체하는 안도 검토했으나 모델 내려받기·VRAM·품질
편차가 따라붙어 "간단히" 가 성립하지 않았다.

**공개 서빙.** 사진은 `uploads/public/` 에 두고 `/api/uploads/` 로 **인증 없이**
내준다. `/people` 이 공개 라우트라 비로그인 방문자의 브라우저가 그냥 받아야 하기
때문이다. 파일명이 UUID 라 주소를 모르면 못 찾지만, **알려진 주소는 계속 열린다.**
회의록 본문 이미지는 반대로 권한을 확인한 뒤에만 내준다 — 두 경로를 한 곳으로
합치지 않은 이유가 이것이다.

**캐시는 1시간.** 내용상으로는 1년 `immutable` 이 맞다(UUID 주소의 바이트는 안
바뀐다). 그런데도 짧게 잡은 건 **내리기가 실제로 먹히게** 하려는 것이다. 앞단이
Cloudflare 라 길게 잡으면 사진을 지워도 캐시 사본이 그만큼 계속 열린다. 사람
얼굴이므로 그 쪽을 택했다. 완전한 회수는 아니라는 점은 남는다.

**딸려 나온 것.** `lib/objectStorage.ts`·`objectAcl.ts`·`storage-serve.ts`·
`admin-people-avatars.ts`·`@workspace/integrations-gemini-ai` 가 통째로 정리돼
**Replit 잔재가 레포에서 사라졌다.** 업로드 파일이 백업에서 빠져 있던 것도 이때
드러나 `ops/backup.sh` 가 `uploads/` 를 함께 뜨도록 고쳤다.


### ADR-008 — 데스크톱 우선, 모바일은 읽기 보장

**결정.** 데스크톱을 기준으로 설계한다. 모바일에서는 **깨지지 않고 읽히는 것**까지만 보장하고, 편집·복잡 인터랙션은 지원하지 않는다. 모바일 레이아웃이 애매해지는 화면은 **데스크톱 전용으로 명시**한다.

**근거.** admin 화면은 표·칸반·분할 편집기가 많아 전면 반응형은 비용이 크고 데스크톱 경험을 타협시킨다.

**대가.** 멘토가 폰에서 상태체크를 못 한다. 본업이 있는 현직 개발자에게는 입력 장벽이다. → ADR-007의 Discord 알림이 "지금 확인이 필요하다"는 신호를 전달하고, 실제 입력은 데스크톱에서 한다. **이 조합이 깨지면(알림 미도입) 상태체크 입력률을 재검토해야 한다.**

---

## 3. 편집 경험

### 3.1 컴포넌트

새 의존성을 추가하지 않는다. 기존 `Textarea` + `react-markdown` 위에 얇은 툴바를 얹은 `MarkdownEditor` 하나를 만든다.

```text
artifacts/seeds/src/components/markdown/
  MarkdownEditor.tsx     툴바 + Textarea + 미리보기 토글
  MarkdownToolbar.tsx    버튼 → 커서 위치에 마크다운 삽입
  useMarkdownInsert.ts   selection 보존 삽입 로직
  MarkdownView.tsx       react-markdown 렌더 (기존 MarkdownBlock 대체)
```

**`@uiw/react-md-editor` 같은 기성 에디터를 쓰지 않는 이유:** 자체 테마를 들고 와서 shadcn/ui와 충돌하고, 다크모드·폰트(Pretendard)를 다시 맞춰야 한다. 툴바는 150줄 남짓이라 직접 만드는 편이 싸다.

### 3.2 툴바 구성

| 그룹 | 버튼 | 단축키 |
|---|---|---|
| 텍스트 | 굵게 · 기울임 · 취소선 · 인라인 코드 | ⌘B · ⌘I · — · ⌘E |
| 블록 | 제목(H2/H3) · 인용 · 코드블록 · 구분선 | — |
| 목록 | 순서 없음 · 순서 있음 · **체크리스트** | ⌘⇧8 · ⌘⇧7 · ⌘⇧9 |
| 삽입 | 링크 · 표 · 이미지 | ⌘K · — · — |

**체크리스트를 툴바 1급으로 올리는 이유:** Ops v3 §6이 요구하는 행사 준비 체크리스트·모집 운영 체크리스트가 전부 `- [ ]` 문법이다. 이게 묻히면 체크리스트 문서 기능이 사실상 안 쓰인다.

### 3.3 이미지 붙여넣기

```text
클립보드 붙여넣기 / 드래그앤드롭
  → POST /admin/attachments  (multipart)
  → attachments 행 생성 (linkedObjectType='document'|'meeting', visibility=admin_only)
  → 응답 URL 을 커서 위치에 ![](url) 삽입
```

⚠️ **[04 §5 `attachments`](04-core-infra.md)에 의존한다.** 그 전까지 툴바는 이미지 버튼을 비활성으로 두고 "외부 링크 붙여넣기"만 안내한다.

### 3.4 편집 모드

| 화면 | 배치 |
|---|---|
| 문서 상세 | 좌우 분할 (편집 │ 미리보기) — **현행 유지** |
| 회의록 섹션 | 섹션별 인라인 편집. 미리보기는 탭 토글 (분할하면 섹션이 너무 좁다) |
| 회고 (학생) | 단일 편집 + 미리보기 토글 |
| 짧은 입력 (상태체크 코멘트 등) | 툴바 없는 plain `Textarea` |

**모든 마크다운 입력에 툴바를 붙이지 않는다.** 한 줄짜리 입력에 툴바는 소음이다.

---

## 4. 회의록 템플릿

### 4.1 유형별 프리셋

공통 필수 = **결정사항 · 액션 아이템**. 아래 표에서 굵은 것이 그 둘이다.

| meetingType | 섹션 프리셋 |
|---|---|
| `general` 일반 | 안건 · 논의 · **결정사항** · 보류 · **액션** |
| `ops` 운영 | 지난 액션 점검 · 이번 주 운영 현황 · 리스크 · **결정사항** · **액션** |
| `planning` 기획 | 배경/문제 · 옵션 검토 · 트레이드오프 · **결정사항** · **액션** |
| `retro` 회고 | 잘된 것 · 아쉬운 것 · 배운 것 · **결정사항** · **액션** |
| `mentor` 멘토 | 팀별 상태 공유 · 멘토 요청사항 · 운영 지원 필요 · **결정사항** · **액션** |
| `external` 외부 | 참석자/소속 · 논의 내용 · 합의사항 · **결정사항** · **액션** |

### 4.2 구현 방식 — 하드코딩하지 않는다

템플릿은 코드가 아니라 **`documents` 테이블의 `is_template=true` 행**으로 둔다.

```text
documents (is_template=true, docType='meeting_note')
  title:      "운영 회의록 템플릿"
  contentMd:  "## 지난 액션 점검\n\n## 이번 주 운영 현황\n..."
  linkedObjectType: 'meeting_type'      ← 신규 화이트리스트 값
  linkedObjectId:   null
```

`meetings` 생성 시 유형을 고르면 해당 템플릿의 `contentMd`를 본문 초안으로 복사한다. 운영진이 `/admin/documents`에서 템플릿을 직접 고칠 수 있고, 코드 배포 없이 반영된다.

### 4.3 스키마 영향 — 마이그레이션 있음

현행 `meetings`는 4개 고정 컬럼(`agendaMd`·`decisionsMd`·`pendingMd`·`notesMd`)이다. 유형별 가변 섹션과 맞지 않는다.

```ts
// meetings 에 추가
bodyMd: text("body_md").notNull().default(""),   // 템플릿 기반 자유 본문
// decisionsMd 는 유지 — 공통 강제 필드이므로 별도 컬럼이 맞다
```

**이관 정책 (비파괴):**
1. `bodyMd` 컬럼을 추가한다. 기존 4개 컬럼은 **삭제하지 않는다.**
2. 백필: `bodyMd = "## 안건\n{agendaMd}\n\n## 보류\n{pendingMd}\n\n## 메모\n{notesMd}"`
3. `decisionsMd`는 그대로 둔다 — 대시보드·감사에서 결정사항만 뽑는 쿼리가 살아 있어야 한다.
4. UI는 `bodyMd` + `decisionsMd` 2필드로 전환한다.
5. 구 컬럼은 한 기수 운영해보고 문제 없으면 제거한다.

**액션 아이템은 지금처럼 별도 다이얼로그를 유지한다.** 본문에서 `- [ ]`를 파싱해 `ops_tasks`로 자동 승격하는 방식은 담당자·마감일을 잡을 수 없어 반쪽이 된다.

---

## 5. 알림

### 5.1 알림 이벤트

| # | 이벤트 | 수신 | 채널 | 시점 |
|---|---|---|---|---|
| N1 | 작업 마감 초과 | 운영진 채널 | Discord + 배지 | 매일 09:00 요약 |
| N2 | 작업이 `blocked` 로 전환 | 운영진 채널 | Discord | 즉시 |
| N3 | **팀 지원 요청** (`needsOpsSupport`) | 운영진 채널 | Discord + 배지 | 즉시 |
| N4 | 상태체크 14일 경과 팀 | 멘토 채널 | Discord | 주 1회 월요일 |
| N5 | 정산 승인 대기 | 운영진 채널 | Discord + 배지 | 매일 요약 |
| N6 | 평가 마감 임박 (모집 기간) | 운영진 채널 | Discord | 마감 3일 전·1일 전 |
| N7 | 다가오는 행사 (D-3) | 운영진 채널 | Discord | 매일 요약에 포함 |

**N3이 가장 중요하다.** 멘토가 올린 지원 요청이 즉시 운영진에게 닿아야 [02 Mentor Workspace](02-mentor-workspace.md)의 30초 상태체크가 의미를 갖는다. 그게 없으면 멘토는 "말해도 아무 일도 안 일어난다"고 학습한다.

**만들지 않는 알림:** 학생 개인 대상 알림, 멘토 개인 DM, 학생 활동 요약, 회고 작성 독촉.

### 5.2 Discord 웹훅 — 새 테이블 없음

```text
SEEDS_DISCORD_OPS_WEBHOOK_URL      운영진 채널
SEEDS_DISCORD_MENTOR_WEBHOOK_URL   멘토 채널 (선택)
```

- 발송 이력은 [04 §6 `communication_logs`](04-core-infra.md)에 `channel='discord'`로 남긴다. **신규 테이블이 필요 없다.**
- 웹훅 URL 미설정 시 조용히 건너뛴다(`logger.debug`). 알림 실패가 본 기능을 막지 않는다.
- 재시도는 1회까지. 실패는 `communication_logs.status='failed'` + `failureReason`.

**페이로드 규칙 (엄수)**

```json
{
  "content": "🔴 팀 지원 요청 · 3기 프로젝트 「알림봇」",
  "embeds": [{
    "description": "담당 멘토가 운영진 지원을 요청했습니다.",
    "url": "https://<host>/admin/projects/42"
  }]
}
```

- 팀명·프로젝트명·기수까지만. **학생 이름·평가 내용·회고·블로커 원문을 넣지 않는다.**
- 링크를 눌러 GrowthHub에서 권한 확인 후 본다.

### 5.3 인앱 배지 — 새 테이블 없음

`GET /admin/ops-dashboard/summary`가 이미 반환하는 값을 재사용한다. 사이드바 메뉴 옆에 숫자만 띄운다.

| 메뉴 | 배지 |
|---|---|
| 작업 | 지연 + blocked 합계 |
| 운영 대시보드 | 미해결 팀 지원 요청 |
| 재정 | 승인 대기 (`finance` 보유자에게만) |

**읽음 처리를 만들지 않는다.** 배지는 "지금 열려 있는 건수"이지 알림함이 아니다. `notifications` 테이블이 필요 없어진다.

### 5.4 스케줄 실행

일일/주간 요약은 별도 워커 없이 처리한다.

```text
GET /internal/cron/daily-digest   (헤더 시크릿 인증)
  → N1 · N5 · N7 집계 → Discord 발송 → communication_logs 기록
```

Replit Scheduled Deployments 또는 외부 cron이 호출한다. 즉시 알림(N2·N3)은 해당 mutation 라우트에서 직접 호출한다.

---

## 6. 반응형 정책

### 6.1 3단계 등급

모든 화면은 셋 중 하나로 분류하고, 신규 화면 추가 시 등급을 명시한다.

| 등급 | 의미 | 대상 |
|---|---|---|
| **A · 모바일 읽기 보장** | 레이아웃 안 깨짐, 가로 스크롤 없음, 내용 전부 읽힘. 편집은 불가 | 학생 전 화면, 멘토 조회 화면, 공개 사이트 |
| **B · 모바일 열람 가능** | 표는 카드로 접히거나 `overflow-x` 스크롤. 최소한의 조작만 | Admin 목록·상세 조회 |
| **C · 데스크톱 전용** | 좁은 화면에서 안내 배너 표시 후 기능 차단 | 칸반, 분할 편집기, 넓은 표, 출석 일괄 입력 |

### 6.2 화면별 등급

```text
A  공개 사이트 전체 · /student/* 전체 · /mentor/teams · /mentor/projects/:id (조회부)
B  /admin 목록 화면 · /admin/*/:id 상세 조회 · /admin/ops-dashboard
C  /admin/tasks (칸반) · /admin/documents/:id (분할 편집) · /admin/meetings/:id (편집 모드)
   /admin/sessions/:id/attendance (일괄 출석) · /mentor/projects/:id 상태체크 폼
```

**구현 시 채운 것.** 위 목록은 대표 화면만 적은 것이어서, 라우트 전량(85개) 분류는
[`artifacts/seeds/src/lib/responsive-tiers.ts`](../../artifacts/seeds/src/lib/responsive-tiers.ts)가
정본이다. 신규 화면은 여기에 행을 추가해야 한다(§6.1의 "등급을 명시한다"가 이것).

구현하면서 위 목록에 없어 판단이 필요했던 것:

| 화면 | 등급 | 근거 |
|---|---|---|
| `/evaluator` · `/evaluator/applications/:id` | **B** | §6.2가 분류하지 않은 축이다. 상세는 이미 `md:grid-cols-3`로 접혀 깨지지 않으므로 C가 막을 대상이 없고, [HANDOFF §5.4](../HANDOFF.md)가 `/evaluator/*` 게이트가 배정 멘토를 잠그는 사고를 경고한다. 루브릭이 넓어져 375px에서 깨지면 재검토 |
| `/login` · `/admin/login` · `/student/login` · `/activate/:token` | **A** | 폰에서 가장 많이 열리는 진입점이다. 여기가 깨지면 아래 전 등급이 무의미해진다 |
| W8 placeholder 8개 | **B** | 실체화될 때 등급 없는 상태로 시작하지 않게 미리 선언. 넓은 표·일괄 편집으로 판명되면 그때 C로 내린다 |

**등급이 갈리는 화면.** `/mentor/projects/:id`(A인데 상태체크 폼만 C)와
`/admin/meetings/:id`(편집만 C, 읽기는 유지)는 페이지가 아니라 **섹션**에 가드를 건다.
`responsive-tiers.ts`의 `MIXED_TIER_SCREENS`가 이 목록이다.
C 섹션의 진입 버튼(문서 `편집`, 회의록 `편집`)은 `lg` 미만에서 **숨긴다** —
누르면 안내만 나오는 버튼은 없는 청중을 약속하는 것과 같다.

### 6.3 C 등급 처리

```tsx
// 좁은 화면에서 차단 + 안내. 빈 화면이나 깨진 레이아웃을 보여주지 않는다.
<DesktopOnly feature="작업 보드">
  <TaskKanban />
</DesktopOnly>
```

> 이 화면은 데스크톱에 최적화되어 있습니다. 넓은 화면에서 열어주세요.

**규칙: 애매하면 C로 내린다.** 어중간한 모바일 대응은 깨진 화면을 보여주는 것보다 낫지 않다.

구현 노트 — 가드는 children을 **렌더하지 않는다**(CSS로 숨기지 않는다). `display:none`으로
가리면 차단된 UI가 DOM에 남아 포커스가 잡히고, 무엇보다 §6.4가 금지한 가로 오버플로를
계속 만들 수 있다. 칸반이 정확히 그런 트리다.
브레이크포인트는 `hooks/use-desktop.tsx`의 `useIsDesktop()`(1024px)이며,
사이드바 드로어를 움직이는 `useIsMobile()`(768px)과 **별개로 둔다** — §6.4가 드로어는
현행 유지라고 정했으므로 한 훅을 공유하면 드로어 기준까지 끌려간다.

### 6.4 기준선

- 브레이크포인트는 Tailwind 기본값을 쓴다. C 등급 차단 기준은 `lg`(1024px) 미만.
- A·B 등급은 **가로 스크롤 금지**. 표는 `overflow-x-auto` 컨테이너 안에서만 스크롤한다.
- 사이드바는 현행 모바일 드로어를 유지한다.
- **내비게이션은 모든 등급에서 동작해야 한다.** A 등급의 "내용 전부 읽힘"은
  화면 하나가 읽히는 것이 아니라 **다른 화면으로 갈 수 있다는 뜻**이다.

> ⚠️ **W11에서 놓쳤다가 나중에 잡은 것.** 헤더형 레이아웃 4개가 nav를
> `hidden md:flex`로 두고 대체 메뉴를 두지 않아, 폰에서는 지금 보는 화면 말고는
> 아무 데도 갈 수 없었다. 특히 `StudentLayout`은 메뉴 13개가 통째로 사라졌는데
> `/student/*`는 A 등급이다. 즉 등급을 부여해놓고 그 약속을 깨고 있었다.
>
> 원인은 감사 방법이었다 — W11에서 **가로 스크롤만** 봤고 "이 화면에서 다른 데로
> 갈 수 있는가"는 보지 않았다. 등급표가 통과라고 말해도 화면은 막다른 길이었다.
> 지금은 `components/layout/MobileNav.tsx`를 Public·Student·Mentor가 공유한다.
> Evaluator는 nav 항목이 하나뿐이고 그것이 로고와 같은 목적지라 달지 않았다.
>
> 브레이크포인트 클래스는 `md:hidden`/`lg:hidden` **완성된 문자열**로 둔다.
> Tailwind는 소스 텍스트를 스캔하므로 `${bp}:hidden`처럼 조립하면 퍼지된다.

---

## 7. 이 문서에서 함께 정한 나머지

물어보지 않고 정했다. 뒤집을 만한 게 있으면 알려달라.

| 항목 | 결정 | 근거 |
|---|---|---|
| 작업 보드 드래그앤드롭 | **도입한다** | 칸반 모양인데 드롭다운으로만 옮기는 건 어긋난다. C 등급(데스크톱 전용)이므로 터치 대응 부담도 없다<br/>구현: **네이티브 HTML5 DnD, 신규 의존성 없음.** 데스크톱 전용이라 포인터 드래그가 요구사항 전부다. 기존 상태 `Select`는 **남겨둔다** — 네이티브 드래그는 키보드로 접근할 수 없어 이것이 유일한 키보드 경로다 |
| 회고 작성 방식 | **자유 서술 + 선택적 프롬프트** | 유형 선택 시 질문 3개를 placeholder로 제안하되 강제하지 않는다. 양식을 강제하면 형식적 답변이 는다 |
| 전역 검색 | **보류** | 데이터가 쌓이기 전 검색 UX는 과설계다. 각 목록 화면의 필터로 충분하다 |
| 신규 운영진 첫 화면 | **`/admin` 대시보드에 온보딩 카드** | 별도 온보딩 화면 대신, `documents`의 온보딩 문서로 연결하는 카드를 담당 기능 역할에 맞춰 띄운다<br/>구현: `components/OnboardingCard.tsx`. `GET /admin/documents?type=onboarding`을 읽으므로 운영진이 문서를 고치면 배포 없이 반영된다. 다음 단계 링크는 보유 `ops_roles`에만 맞춰 보여준다 — 403이 나는 "첫 단계"는 안내가 없느니만 못하다. `program_lead`는 전 기능을 함의하므로 7종을 다 늘어놓지 않고 자기 진입점 2개만 보인다. 닫기는 localStorage(스키마 불필요, 브라우저별이라는 한계는 감수) |
| 액션 아이템 자동 추출 | **하지 않는다** | 담당자·마감일이 빠져 반쪽이 된다 (§4.3) |

---

## 8. 수용 기준

2026-08-19 확인. **확인한 것만 체크한다** — 미체크는 아직 못 봤다는 뜻이지
"안 된다"는 뜻이 아니다.

- [x] 마크다운을 모르는 사람이 툴바만으로 제목·목록·체크리스트·링크를 넣을 수 있다.
      → `MarkdownEditor.tsx` 툴바 13종(굵게·기울임·취소선·코드·제목2·제목3·인용·
      구분선·목록·번호목록·체크리스트·링크·표).
- [x] 툴바 삽입 후 커서 위치와 선택 영역이 보존된다. → `setSelectionRange` 로 복원.
- [x] 이미지 붙여넣기가 `attachments`로 업로드되고 `admin_only`로 저장된다.
      → `onPaste` → `POST /admin/attachments/upload`. 가시성을 안 보내면 서버가
      `admin_only` 로 잡는다(`attachments.ts`). 저장은 디스크의 **비공개 영역**이라
      주소를 알아도 그냥 열리지 않는다(무인증은 `uploads/public/` 뿐, ADR-017).
- [x] 회의 유형을 고르면 해당 템플릿 본문이 초안으로 채워진다.
      → `bodyMd: d.bodyMd ?? await templateBodyFor(meetingType)`.
- [x] 템플릿을 `/admin/documents`에서 수정하면 다음 회의부터 반영된다 (코드 배포 없이).
      → 템플릿은 `documents`의 `is_template=true` 행이다. 코드에 없다.
- [ ] `meetings` 마이그레이션 후 기존 회의록의 안건·보류·메모 내용이 `bodyMd`에서 전부 보인다.
      → **확인할 데이터가 없다.** 12행 전부 `agenda_md`·`notes_md`·`pending_md` 가
      비어 있어(길이 0) 이관될 내용 자체가 없었다. 실기수 데이터가 들어오기 전까지
      이 기준은 검증되지 않은 채로 둔다 — 빈 것을 보고 통과로 적으면 거짓이 된다.
- [x] `decisionsMd`가 유지되어 결정사항 추출 쿼리가 회귀 없이 동작한다.
      → `meetings.decisions_md` 컬럼 존치 확인.
- [ ] 멘토가 `needsOpsSupport` 상태체크를 올리면 운영진 Discord 채널에 즉시 뜬다.
      → **구현돼 있지 않다.** `notifyDiscord` 호출부는 `internal-cron.ts` 의 정기
      다이제스트 두 곳뿐이고, 상태체크를 저장하는 라우트에는 없다. 그 건수는
      **다음 날 09:00 요약에 개수로만** 들어간다. 즉시 알림이 필요하면 따로 만들어야
      한다 — 웹훅을 넣어도 오지 않는다.
- [x] Discord 페이로드에 학생 이름·평가·회고·블로커 원문이 **없다**.
      → 2026-08-20 실측. 실제로 나가는 JSON 을 받아서 확인했다.
      ```json
      {"content":"오늘의 운영 요약 — 🔴 팀 지원 요청 1건",
       "embeds":[{"description":"GrowthHub 운영 대시보드에서 자세히 확인하세요.",
                  "url":"https://seeds.harvester.kr/admin/ops-dashboard"}]}
      ```
      개수와 링크뿐이다. 링크도 로그인해야 열린다.
- [x] 웹훅 URL 미설정 상태에서 모든 기능이 정상 동작한다.
      → `notifyDiscord` 가 URL 없으면 debug 로그만 남기고 `false` 를 돌려준다.
      전체 검증 33/33 통과가 이 상태에서 나온 결과다.
- [x] Discord 발송이 `communication_logs`에 남고, 실패 시 `failureReason`이 기록된다.
      → 2026-08-20 양쪽 다 실측. 성공은 `sent`, 실패는 `failed` + `HTTP 500`.
      **실패 행은 `sent_at` 이 `null`** 이므로 `sent_at` 으로 거르면 안 보인다 —
      처음에 그렇게 조회해서 "기록이 안 된다" 고 오판했다. 조회는 `created_at` 으로.

> **설정 완료(2026-08-20).** 운영·멘토 채널을 나누지 않고 공용 웹훅 하나로 받는다.
> `SEEDS_DISCORD_WEBHOOK_URL` 과 `CRON_SECRET` 은 `~/.secrets/seeds-preview.env`
> (600, git 미추적)에 있다. 채널을 나누려면 `SEEDS_DISCORD_OPS_WEBHOOK_URL` /
> `_MENTOR_` 를 넣으면 되고 그쪽이 먼저 적용된다.
- [x] 학생 개인에게 가는 알림이 **없다**.
      → 2026-08-24 실측. `notifyDiscord` 호출부 2곳 모두 채널 대상이고,
      `/student/notifications` 는 404(그런 경로가 없다).
- [x] A 등급 화면이 375px에서 가로 스크롤 없이 읽힌다.
      → **브라우저 실측 완료** (Playwright Chromium, 2026-08-03).
      공개 6개(`/`·`/about`·`/program`·`/people`·`/recruit`·`/faq`)와
      학생 4개(`/student`·`/reflections`·`/attendance`·`/report`) 전부
      `scrollWidth=375 == clientWidth=375`. 오버플로 0건.
- [x] C 등급 화면이 좁은 화면에서 깨진 레이아웃 대신 안내를 보여준다.
      → **브라우저 실측 완료.** 900px에서 `/admin/tasks`가 안내만 렌더하고
      보드(`task-column-*`)는 DOM에 없다 — CSS 숨김이 아니라 미렌더임이 확인됐다.
      문구도 §6.3 그대로. 1440px에서는 정상 렌더.
- [x] 작업 카드를 드래그해 다른 컬럼으로 옮길 수 있다.
      → **브라우저 실측 완료.** 1440px에서 카드가 `todo → review → done`으로
      이동했고, 실행 간에 상태가 유지되어 **서버에 반영**됨이 확인됐다.
- [x] 폰에서 각 역할의 내비게이션이 동작한다.
      → 공개 5개·학생 13개 링크가 드로어에 전부 나오고, 이동 시 자동 닫힘·Esc 닫힘 동작.

> 검증 하네스는 `e2e/`에 있다. `BASE_URL`을 바꾸면 로컬·프리뷰 어디에나 붙는다.

## 9. 미결

| 항목 | 결정 시점 |
|---|---|
| 표 편집 UX가 툴바만으로 부족한지 | 운영진 실사용 1개월 후 |
| 이메일 알림을 추가할지 (Discord 미가입 멘토 대응) | 멘토 온보딩 시 |
| 알림 빈도가 과한지 (채널 피로) | 첫 기수 운영 후 |
| 멘토 상태체크 입력률이 데스크톱 전용으로 충분한지 | 첫 기수 운영 후 — **미달 시 ADR-008 재검토** |
| 구 `meetings` 4컬럼 제거 시점 | 이관 후 한 기수 |
