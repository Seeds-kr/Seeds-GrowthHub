# Seeds GrowthHub

학생 개발자 동아리 **Seeds** 운영 플랫폼. 공개 사이트 · 지원/심사 · 기수 운영 ·
학생 활동기록을 한 레포에서 다룬다.

## 시작하기 전에 — 순서대로

1. **[docs/gotchas.md](docs/gotchas.md)** — 한 번 당한 것만 적혀 있다. 여기서
   시간을 잃은 사람이 이미 있다는 뜻이다.
2. **[README.md](README.md)** — 무엇이 어디 있는지. 라우트·스키마·불변식.
3. **[docs/design/](docs/design/)** — 왜 그렇게 됐는지. ADR 이 여기 있다.

## 이 레포에서 반복해 나오는 것

**문서와 주석이 코드보다 앞서간다.** 2026-08 한 세션에서만 이런 게 여럿 나왔다 —
시드가 "여러 번 돌려도 안전하다" 고 약속만 하고 안 지킴, 백업 주석이 뜨지도 않는
디렉터리를 뜬다고 함, 수용 기준 11개가 전부 미체크인데 8개는 이미 구현돼 있었음,
AI 아바타가 죽은 채로 문서에는 살아 있는 기능으로 적혀 있었음.

**그래서 여기서는 "적혀 있다" 를 근거로 쓰지 않는다.** 확인하고 적는다. 확인 못 한
것은 **미체크로 남기고 이유를 쓴다** — 빈 것을 보고 통과로 적으면 다음 사람이 그걸
믿는다.

## 검증

```bash
bash ops/verify.sh          # 화면 · 스토리 33개 · 전 라우트. 진입점은 이것 하나다
bash ops/verify-restore.sh  # 백업이 정말 복원되는지 (라이브 안 건드림)
```

**`node e2e/stories.mjs` 를 직접 돌리지 않는다.** `E2E_ADMIN_*` 이 없어 운영진
스토리가 통째로 BLOCK 되고, 제품이 멀쩡한데 회귀처럼 보인다(gotchas 참고).

로그인 제한은 **IP|이메일당 5분 10회**다. 손으로 검증 로그인을 섞은 직후 검증을
돌리면 진짜로 걸린다.

## 손대기 전에 확인받을 것

- 프로덕션/프리뷰 **DB 삭제·마이그레이션**. 파괴적 SQL 은 사본 DB
  (`CREATE DATABASE … TEMPLATE growthhub`)에서 먼저 돌려 본다.
- **PR 머지**. "검토해줘" 는 리뷰까지다.
- **알림 발송 범위**를 넓히는 변경. 채널에 글이 더 자주 뜨는 것은 제품 결정이다.

## 신원

커밋은 **DDuckyee** 로 한다. 서버 기본 계정이 동료라 그대로 두면 남의 이름으로 남는다.

```bash
git -c user.name=DDuckyee -c user.email=dduckyee03@gmail.com commit …
```

`main` 이 배포 브랜치다. 작업은 브랜치를 따서 PR 로 올린다.
